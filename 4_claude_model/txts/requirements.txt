# FYDP Project — Python Dependencies
# Install with: pip install -r requirements.txt
#
# Core data
numpy>=1.24
pandas>=2.0
openpyxl>=3.1          # read/write .xlsx files
scipy>=1.11            # statistics, optimisation

# Visualisation
matplotlib>=3.7
seaborn>=0.12

# Machine learning (preprocessing + metrics only — LSTM built from numpy)
scikit-learn>=1.3

# Optional — install these for faster/better LSTM training
# tensorflow>=2.13       # replace numpy LSTM with Keras in nb2
# torch>=2.0             # alternative deep learning backend
# arch>=6.3              # proper GARCH via MLE (replaces numpy GARCH in nb3)
# statsmodels>=0.14      # proper ARIMA + Ljung-Box (replaces manual implementations)
# cvxpy>=1.4             # convex portfolio optimisation (replaces scipy.optimize)

# ── Quick install (core only, no GPU) ─────────────────────────────────────
# pip install numpy pandas openpyxl scipy matplotlib seaborn scikit-learn
