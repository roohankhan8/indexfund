# Overview

The detailed repository documentation now lives at the project root:

- `README.md` for the cleaned top-level structure
- `PROJECT_REFERENCE.md` for the full project explanation

Current canonical locations:

- `7_codex_model/` for the main analysis pipeline
- `report_workspace_2/` for the main report workspace

This `overview/` folder remains a lightweight summary area plus supporting figures.
