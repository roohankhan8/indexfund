# .ai Agent Guidance for 8_last_model

- Primary script: `pipeline.py`
- Keep all new code in this folder self-contained and path-relative.
- Do not modify raw files under `data/`.
- Save artifacts only under `output/` subfolders.
- If changing features/models, update `mds/8_last_model_workflow.md`.
- Maintain deterministic seeds (`random_state=42`) for comparable report outputs.
